name: CI

on:
  push:
    branches:
      - main
    paths-ignore:
      - docs/**
      - "**/*.md"
      - "LICENSE"
    tags:
      - v**
  pull_request:
    paths-ignore:
      - docs/**
      - "**/*.md"
      - "LICENSE"

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  JAVA_VERSION: 27

permissions:
  checks: write

jobs:
  # ====================
  # Code Quality
  # ====================
  rustfmt:
    name: Code Quality / rustfmt
    runs-on: ubuntu-latest
    steps:
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
          components: rustfmt
      - name: Checkout source code
        uses: actions/checkout@v7
      - name: Check code formatting with rustfmt
        run: cargo fmt --all -- --check

  clippy:
    name: Code Quality / clippy
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
          components: clippy
      - name: Setup Checking Tools
        uses: taiki-e/install-action@v2.87.13
        with:
          tool: clippy-sarif,sarif-fmt
      - name: Setup JDK
        uses: actions/setup-java@v6.0.1
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: corretto
      - name: Checkout source code
        uses: actions/checkout@v7
      - name: Cache Rust Build Stuff
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/.crates.toml
            ~/.cargo/.crates2.json
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-${{ runner.arch }}-cargo-target-${{ github.job }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-cargo-target-${{ github.job }}-
            ${{ runner.os }}-${{ runner.arch }}-cargo-target-
      - name: Run clippy
        run: |
          set -o pipefail
          cargo clippy \
            --all-features --all-targets \
            --message-format=json \
            -- -D warnings 2>&1 | clippy-sarif | tee rust-clippy-results.sarif | sarif-fmt
          echo "CLIPPY_EXIT_CODE=${PIPESTATUS[0]}" >> "$GITHUB_ENV"
        continue-on-error: true
      - name: Upload analysis results to GitHub
        uses: github/codeql-action/upload-sarif@v4.38.0
        if: always()
        with:
          sarif_file: rust-clippy-results.sarif
          wait-for-processing: true
      - name: Check clippy result
        if: always()
        run: |
          if [ "$CLIPPY_EXIT_CODE" != "0" ]; then
            echo "Clippy found issues (exit code: $CLIPPY_EXIT_CODE)"
            exit $CLIPPY_EXIT_CODE
          fi
      - name: Sweep target cache
        if: always()
        uses: BD103/cargo-sweep@v2
        with:
          manifest-path: Cargo.toml

  # ====================
  # Tests
  # ====================
  feature_test:
    name: Tests / Feature Combinations
    runs-on: ubuntu-latest
    needs: [rustfmt, clippy]
    steps:
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
      - name: Setup cargo-hack
        uses: taiki-e/install-action@v2.87.13
        with:
          tool: cargo-hack
      - name: Checkout source code
        uses: actions/checkout@v7
      - name: Cache Rust Build Stuff
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/.crates.toml
            ~/.cargo/.crates2.json
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-${{ runner.arch }}-cargo-target-${{ github.job }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-cargo-target-${{ github.job }}-
            ${{ runner.os }}-${{ runner.arch }}-cargo-target-
      - name: Test all feature combinations
        env:
          RUSTFLAGS: -D unused -D unfulfilled_lint_expectations
          MOKAPOT_SKIP_JAVA_TESTS: 1
        run: |
          cargo hack check --feature-powerset --no-dev-deps
      - name: Sweep target cache
        if: always()
        uses: BD103/cargo-sweep@v2
        with:
          manifest-path: Cargo.toml

  unit_test:
    name: Tests / Unit Tests
    runs-on: ubuntu-latest
    needs: [rustfmt, clippy]
    steps:
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
          components: llvm-tools-preview
      - name: Setup Build Tools
        uses: taiki-e/install-action@v2.87.13
        with:
          tool: cargo-llvm-cov,cargo-nextest
      - name: Checkout source code
        uses: actions/checkout@v7
      - name: Cache Rust Build Stuff
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/.crates.toml
            ~/.cargo/.crates2.json
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-${{ runner.arch }}-cargo-target-${{ github.job }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-cargo-target-${{ github.job }}-
            ${{ runner.os }}-${{ runner.arch }}-cargo-target-
      - name: Test
        env:
          MOKAPOT_SKIP_JAVA_TESTS: 1
        run: |
          cargo llvm-cov clean --workspace
          cargo llvm-cov nextest --all-features --no-report -E 'kind(lib)'
          cargo llvm-cov report --codecov --output-path ./target/codecov-unit.json
      - name: Upload Coverage Data
        uses: actions/upload-artifact@v7
        with:
          name: codecov-unit.json
          path: ./target/codecov-unit.json
      - name: Sweep target cache
        if: always()
        uses: BD103/cargo-sweep@v2
        with:
          manifest-path: Cargo.toml

  fixture_test:
    name: Tests / Java Fixtures
    runs-on: ubuntu-latest
    needs: [rustfmt, clippy]
    steps:
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
          components: llvm-tools-preview
      - name: Setup Build Tools
        uses: taiki-e/install-action@v2.87.13
        with:
          tool: cargo-llvm-cov,cargo-nextest
      - name: Setup JDK
        uses: actions/setup-java@v6.0.1
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: corretto
      - name: Checkout source code
        uses: actions/checkout@v7
      - name: Cache Rust Build Stuff
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/.crates.toml
            ~/.cargo/.crates2.json
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-${{ runner.arch }}-cargo-target-${{ github.job }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-cargo-target-${{ github.job }}-
            ${{ runner.os }}-${{ runner.arch }}-cargo-target-
      - name: Test
        timeout-minutes: 10
        run: |
          cargo llvm-cov clean --workspace
          cargo llvm-cov nextest --all-features --no-report -E 'kind(test)'
          cargo llvm-cov report --codecov --output-path ./target/codecov-fixtures.json
      - name: Upload Coverage Data
        uses: actions/upload-artifact@v7
        with:
          name: codecov-fixtures.json
          path: ./target/codecov-fixtures.json
      - name: Sweep target cache
        if: always()
        uses: BD103/cargo-sweep@v2
        with:
          manifest-path: Cargo.toml

  jdk_smoke:
    name: Tests / JDK Smoke
    runs-on: ubuntu-latest
    needs: [rustfmt, clippy]
    steps:
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
          components: llvm-tools-preview
      - name: Setup Build Tools
        uses: taiki-e/install-action@v2.87.13
        with:
          tool: cargo-llvm-cov,cargo-nextest
      - name: Setup JDK
        uses: actions/setup-java@v6.0.1
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: corretto
      - name: Checkout source code
        uses: actions/checkout@v7
      - name: Hash JDK modules
        id: hash-jdk-modules
        run: |
          file_path="${{ env.JAVA_HOME }}/lib/modules"
          hash=$(sha256sum "$file_path" | awk '{print $1}')
          echo "hash=${hash}" >> "$GITHUB_OUTPUT"
      - name: Cache JDK classes
        id: cache-jdk-classes
        uses: actions/cache@v6
        with:
          key: ${{ runner.os }}-${{ runner.arch }}-jdk-classes-${{ steps.hash-jdk-modules.outputs.hash }}
          path: jdk_classes/
      - name: Extract JDK classes
        if: steps.cache-jdk-classes.outputs.cache-hit != 'true'
        run: jimage extract --verbose --dir=./jdk_classes "$JAVA_HOME/lib/modules"
      - name: Cache Rust Build Stuff
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/.crates.toml
            ~/.cargo/.crates2.json
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-${{ runner.arch }}-cargo-target-${{ github.job }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-cargo-target-${{ github.job }}-
            ${{ runner.os }}-${{ runner.arch }}-cargo-target-
      - name: Test
        env:
          MOKAPOT_SKIP_JAVA_TESTS: 1
          JDK_CLASSES: ${{ github.workspace }}/jdk_classes
        timeout-minutes: 10
        run: |
          cargo llvm-cov clean --workspace
          cargo llvm-cov nextest --cargo-profile=jdk-smoke --all-features --test jdk_classes --no-report --run-ignored=all
          cargo llvm-cov report --profile=jdk-smoke --codecov --output-path ./target/codecov-jdk-smoke.json
      - name: Upload Coverage Data
        uses: actions/upload-artifact@v7
        with:
          name: codecov-jdk-smoke.json
          path: ./target/codecov-jdk-smoke.json
      - name: Sweep target cache
        if: always()
        uses: BD103/cargo-sweep@v2
        with:
          manifest-path: Cargo.toml

  miri_test:
    name: Tests / Miri
    runs-on: ubuntu-latest
    needs: [rustfmt, clippy]
    steps:
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly
          components: miri
      - name: Setup cargo-nextest
        uses: taiki-e/install-action@v2.87.13
        with:
          tool: cargo-nextest
      - name: Checkout source code
        uses: actions/checkout@v7
      - name: Cache Rust Build Stuff
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-${{ runner.arch }}-cargo-target-${{ github.job }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-cargo-target-${{ github.job }}-
      - name: Setup Miri sysroot
        run: cargo +nightly miri setup
      - name: Test with Miri
        env:
          MIRIFLAGS: -Zmiri-env-set=PROPTEST_CASES=4 -Zmiri-env-set=PROPTEST_DISABLE_FAILURE_PERSISTENCE=1
          MOKAPOT_SKIP_JAVA_TESTS: 1
        run: cargo +nightly miri nextest run -p mokapot --all-features --lib --no-tests=fail

  codecov:
    name: Tests / Coverage Report
    runs-on: ubuntu-latest
    needs: [unit_test, fixture_test, jdk_smoke]
    steps:
      - name: Checkout source code
        uses: actions/checkout@v7
      - name: Download Coverage Data (Unit Test)
        uses: actions/download-artifact@v8
        with:
          name: codecov-unit.json
      - name: Download Coverage Data (Java Fixtures)
        uses: actions/download-artifact@v8
        with:
          name: codecov-fixtures.json
      - name: Download Coverage Data (JDK Smoke)
        uses: actions/download-artifact@v8
        with:
          name: codecov-jdk-smoke.json
      - name: Upload to codecov (Unit Test)
        uses: codecov/codecov-action@v7
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
        with:
          files: codecov-unit.json
          flags: unit_tests
          fail_ci_if_error: true
      - name: Upload to codecov (Java Fixtures)
        uses: codecov/codecov-action@v7
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
        with:
          files: codecov-fixtures.json
          flags: fixture_tests
          fail_ci_if_error: true
      - name: Upload to codecov (JDK Smoke)
        uses: codecov/codecov-action@v7
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
        with:
          files: codecov-jdk-smoke.json
          flags: jdk_smoke
          fail_ci_if_error: true

  # ====================
  # Documentation
  # ====================
  build_doc:
    name: Docs / Build Rustdoc
    runs-on: ubuntu-latest
    needs: [unit_test]
    steps:
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly
      - name: Checkout source code
        uses: actions/checkout@v7
      - name: Build docs
        env:
          RUSTDOCFLAGS: --cfg docsrs
        run: cargo doc --all-features --no-deps --verbose
      - name: Fix file permissions
        run: |
          chmod -v -R +rX "target/doc/" | while read -r line; do
            echo "::info title=Fixed file permissions::$line"
          done
      - name: Upload docs as github pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          name: mokapot-docs
          path: target/doc

  deploy_docs:
    name: Docs / Deploy to GitHub Pages
    runs-on: ubuntu-latest
    needs: [feature_test, unit_test, fixture_test, jdk_smoke, miri_test, build_doc]
    if: ${{ github.ref == 'refs/heads/main' }}
    environment:
      name: Latest Docs
      url: https://henryhchchc.github.io/mokapot/mokapot
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy docs to github pages
        uses: actions/deploy-pages@v5
        with:
          artifact_name: mokapot-docs

  # ====================
  # Publish
  # ====================
  publish:
    name: Deploy / crates.io
    runs-on: ubuntu-latest
    if: ${{ startsWith(github.ref, 'refs/tags/v') }}
    needs: [feature_test, unit_test, fixture_test, jdk_smoke, miri_test, build_doc]
    environment:
      name: crates.io
      url: https://crates.io/crates/mokapot
    permissions:
      id-token: write
    strategy:
      matrix:
        crate: ["mokapot"]
    steps:
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
      - name: Checkout source code
        uses: actions/checkout@v7
      - name: Authenticate crates.io
        uses: rust-lang/crates-io-auth-action@v1
        id: auth
      - name: Publish to crates.io
        working-directory: crates/${{ matrix.crate }}
        run: cargo publish --all-features --verbose
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
